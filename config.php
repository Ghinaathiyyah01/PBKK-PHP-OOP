<?php
// Definisi kelas 'config' untuk konfigurasi koneksi database
class Config {
    // Properti hostname digunakan untuk menyimpan alamat host database
    protected $hostname = 'localhost';

    // Properti username digunakan untuk menyimpan nama pengguna database
    protected $username = 'root';

    // Properti password digunakan untuk menyimpan kata sandi pengguna database
    protected $password = '';

    // Properti database digunakan untuk menyimpan nama database yang akan digunakan
    protected $database = 'barang';
}
